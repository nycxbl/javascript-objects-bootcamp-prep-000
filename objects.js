
var playlist = {
  'Pokemon': 'Pokemon Song',
};

function updatePlaylist(list, artist, title) {
  returnObject.assign({}, {artist: tite});
}

function removeFromPlaylist(list, artist) {
  delete list[artist];
  return list;
}